/** @format */

import { Dialog } from "@headlessui/react";

const CreateLoanModal = ({ open }: { open: boolean }) => {
  return <Dialog open={open} onClose={() => {}}></Dialog>;
};

export default CreateLoanModal;
